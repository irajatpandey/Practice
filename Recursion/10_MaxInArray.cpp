#include<bits/stdc++.h>
using namespace std;

void maxInArray(int *arr, int s, int n){
    if(s == n) return;
    int ans = max(arr[s], )
}
int main() {
    int n; cin >> n;
    int arr[n];
    for(int i = 0; i < n; i++) cin >> arr[i];

    cout << maxInArray(arr, 0, n);
    return 0;
}